# Cloud Dimensions - ServiceNow CI/CD lab pipeline
# Branch: lab3-start  (Lab 3 / Lesson 6 - Build a GitHub Actions Workflow)
# Starting state: trigger and checkout only. Add the job-level pipeline credentials and
# the apply-changes step (with its own step-level instance variable).
name: CICD Pipeline

on:
  push:
    branches: [lab3-start]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      # TODO: Add the pipeline credentials (job-level env block) and the apply changes step here
